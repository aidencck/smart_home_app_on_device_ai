name: on_device_agent
description: A local package for On-Device AI Agent logic in the Smart Home App.
version: 0.0.1
publish_to: 'none' # This is an internal package.

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.2
  isar: ^3.1.0+1
  crypto: ^3.0.7
  http: ^1.6.0
  local_auth: ^3.0.1
  system_info2: ^4.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.4.13
  isar_generator: ^3.1.0+1
